{
  "_comment": "Local-dev-only defaults. Never used when ASPNETCORE_ENVIRONMENT=Production — override everything via env vars there (see .env.example).",
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=momentum;Username=momentum;Password=momentum"
  },
  "Jwt": {
    "Secret": "dev-only-insecure-secret-change-me-32-bytes-min"
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug"
    }
  }
}
